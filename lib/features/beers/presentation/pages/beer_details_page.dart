import 'package:flutter/material.dart';

class BeerDetailsPage extends StatelessWidget {
  const BeerDetailsPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
